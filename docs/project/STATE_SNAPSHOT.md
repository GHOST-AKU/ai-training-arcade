# 状态快照

- 日期：2026-09-22。
- current_phase：方向首页开发完成，等待用户视觉验收。
- 基线：远程 `main` / `e970450`。
- 当前工作分支：`chore/organize-project`。
- 原工作分支：`agent/add-lab-difficulty` / `d4cc9fe`，已保留。
- 当前范围：新增方向首页，将现有训练场入口移至 machine-learning.html；深度学习和强化学习显示施工中。补齐双语、返回导航及资源版本。
- 整理阶段历史验证：`npm test` 通过，包含 36 个 JavaScript 文件语法检查、八个训练场结构契约、难度、26 关可达性及预算检查、中英文覆盖、模型与页面 smoke；该结果不是新首页的全量验证。
- 其他验证：`npm run benchmark`、`npm run playability -- --report` 和 `git diff --check` 通过；报告已生成到 `docs/reports/playability-report.json`。
- 首页改动：HTML、独立首页 CSS、共享导航、翻译和缓存版本；未改动算法、关卡或部署 workflow。
- 验证边界：未执行可选 JSDOM 回归、真实浏览器、截图或视觉验收。
- 首页验证：变更脚本语法和中英文覆盖通过；结构检查同步为机器学习板块页入口。未运行全量测试或性能基准，视觉验收交由用户。
- 随机森林后续修复：宽屏右侧 Canvas 按实际树行数排列说明、投票区和趋势图，避免独立固定坐标重叠；仅做 forest.js 语法及差异格式检查，等待用户视觉验收。
- 发布范围：用户已授权提交并推送整理分支；实际提交与远程同步状态以 Git 为准，未授权合并或部署。
- 后续协作：仅做与改动相关的必要检查，复用有效验证结果；视觉测试与视觉验收交由用户进行。
- 统一反馈组件：八个训练场的提示框改为标题下的无底色反馈行，左侧固定标签、右侧动态消息、底部细分隔线；撤销随机森林独有样式，保留原提示更新和无障碍播报，视觉效果待用户验收。

后续接手先运行 `git status --short --branch`，不要把本快照的分支与验证状态当成永远有效。工作目录名称沿用历史名称 `gbm-gradient-game`，项目正式名称为 AI Training Arcade。
