# 结构决策

## 2026-09-22：整理开发资料，保留站点路径

- 授权依据：用户要求整理项目结构，为接下来的工作做准备。
- 基线：远程 `main` 的 `e970450`；原本地分支 `agent/add-lab-difficulty` 的 `d4cc9fe` 是其祖先，无未提交改动。
- 决定：在 `chore/organize-project` 整理，保留原分支。按职责区分 scripts、tests、benchmarks 及 docs 子目录。
- 原因：原 scripts 混放服务器、测试辅助、规则检查和性能脚本；历史 QA 与当前规范缺少区分，贡献文档缺少结构入口。
- 影响：内部开发脚本和文档路径变化；npm 命令名、站点运行路径和功能保持不变。可玩性报告生成位置同步迁移。
- 风险：直接调用旧脚本路径的外部命令需要更新。README、贡献指南和开发说明已提供新入口。
- 回退：原分支和 `e970450` 保留。需要回退时先保存整理分支上的工作，再切回基线；不要用全盘清理或强制重置处理其他人的改动。
- 后续授权：用户要求提交并推送整理分支；不包含合并或部署。

## 2026-09-22：控制验证成本

- 日常只做与改动直接相关的必要检查，不默认运行全量、重复或性能测试。
- 视觉测试与视觉验收由用户负责，AI 不主动执行；另有明确要求时再执行。
- 保留现有测试工具和 CI 配置，本次推送复用上一轮验证结果。
